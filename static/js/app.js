// Generated by CoffeeScript 1.9.1
(function() {
  var buildAddView, buildTableView, cipher, compress, database, db_url, decipher, decompress, emptyMain, get_url, password, save, send_via_post;

  db_url = "/get";

  database = void 0;

  sjcl.random.startCollectors();

  compress = function(message) {
    return Base64.toBase64(RawDeflate.deflate(Base64.utob(message)));
  };

  decompress = function(data) {
    return Base64.btou(RawDeflate.inflate(Base64.fromBase64(data)));
  };

  cipher = function(key, message) {
    return sjcl.encrypt(key, JSON.stringify(message));
  };

  decipher = function(key, data) {
    return JSON.parse(sjcl.decrypt(key, data));
  };

  get_url = function(url, feedback, error) {
    var req;
    req = new XMLHttpRequest();
    req.open('GET', url, true);
    req.onreadystatechange = function(aEvt) {
      if (req.readyState === 4) {
        if (req.status === 200) {
          return feedback(req.responseText);
        } else {
          return error();
        }
      }
    };
    return req.send(null);
  };

  send_via_post = function(url, data, feedback, error) {
    var req;
    req = new XMLHttpRequest();
    req.open('POST', url, true);
    data = "data=" + encodeURIComponent(data);
    req.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
    req.setRequestHeader("Content-length", data.length);
    req.setRequestHeader("Connection", "close");
    req.onreadystatechange = function(aEvt) {
      if (req.readyState === 4) {
        if (req.status === 200) {
          return feedback(req.responseText);
        } else {
          return error();
        }
      }
    };
    return req.send(data);
  };

  save = function(cb) {
    var data;
    data = cipher(password, window.database);
    console.log(decipher(password, data));
    return send_via_post("/save", data, function(success) {
      console.log("ok");
      if (cb) {
        return cb();
      }
    }, function() {
      return console.log(":(");
    });
  };

  emptyMain = function() {
    var c, i, len, myNode, ref, results;
    myNode = document.getElementsByTagName("main")[0];
    ref = myNode.children;
    results = [];
    for (i = 0, len = ref.length; i < len; i++) {
      c = ref[i];
      results.push(myNode.removeChild(c));
    }
    return results;
  };

  buildTableView = function() {
    var e, el, i, j, l, len, len1, main, newpass, ref, ref1, table;
    main = document.getElementsByTagName("main")[0];
    main.innerHTML = "";
    main.innerHTML += "<p><a href=\"\" id=\"add-a\">New password</a></p><table><tr><th>Site</th><th>Password</th><th>Edit</th><th>Delete</th></tr> </table>";
    table = document.querySelector("main table");
    ref = window.database;
    for (i = 0, len = ref.length; i < len; i++) {
      e = ref[i];
      l = document.createElement("tr");
      l.innerHTML = "<form><tr><td><input readonly name='website' type=\"text\" value=\"" + e.website + "\" /></td><td><input readonly name='password' class='text-hidden' type=\"text\" value=\"" + e.password + "\" /></td><td><button class=\"edit-a\">Edit</button></td><td><button class=\"delete-a\">Delete</button></td></tr></form>";
      l.edited = false;
      l.getElementsByClassName("edit-a")[0].addEventListener("click", (function(l, e) {
        return function(evt) {
          var element, input, j, k, len1, len2, len3, m, ref1, ref2, ref3;
          if (l.edited) {
            ref1 = l.getElementsByTagName("input");
            for (j = 0, len1 = ref1.length; j < len1; j++) {
              element = ref1[j];
              element.setAttribute("disabled", null);
            }
            ref2 = l.getElementsByTagName("input");
            for (k = 0, len2 = ref2.length; k < len2; k++) {
              input = ref2[k];
              if (input.name === "website") {
                e.website = input.value;
              }
              if (input.name === "password") {
                e.password = input.value;
              }
            }
            save(function() {
              return this.innerHTML = "Saved";
            });
          } else {
            ref3 = l.getElementsByTagName("input");
            for (m = 0, len3 = ref3.length; m < len3; m++) {
              element = ref3[m];
              element.removeAttribute("disabled");
            }
            this.innerHTML = "Ok";
          }
          return l.edited = !l.edited;
        };
      })(l, e));
      l.getElementsByClassName("delete-a")[0].addEventListener("click", (function(l, e) {
        return function(evt) {
          window.database.splice(window.database.indexOf(e), 1);
          return save(function() {
            return buildTableView();
          });
        };
      })(l, e));
      table.appendChild(l);
    }
    newpass = document.createElement("tr");
    newpass.innerHTML = "<form><td><input name='website' type='text' placeholder='Website' /></td><td><input name='password' type='text' value='aaaa' /></td><td><button class=\"add\">Add</button></td></form>";
    table.appendChild(newpass);
    newpass.getElementsByClassName("add")[0].addEventListener("click", function(evt) {
      var input, j, len1, ref1;
      e = {};
      ref1 = newpass.getElementsByTagName("input");
      for (j = 0, len1 = ref1.length; j < len1; j++) {
        input = ref1[j];
        if (input.name === "website") {
          e.website = input.value;
        }
        if (input.name === "password") {
          e.password = input.value;
        }
      }
      console.log(window.database);
      window.database.push(e);
      console.log(window.database);
      return save(function() {
        return buildTableView();
      });
    });
    ref1 = table.getElementsByClassName('text-hidden');
    for (j = 0, len1 = ref1.length; j < len1; j++) {
      el = ref1[j];
      el.addEventListener('click', function(evt) {
        return this.select();
      });
      el.addEventListener('dbclick', function(evt) {
        return this.select();
      });
    }
    return document.getElementById("add-a").addEventListener("click", function(evt) {
      evt.preventDefault();
      return buildAddView();
    });
  };

  buildAddView = function() {
    var html;
    emptyMain();
    html = "<form>";
    html += "</form>";
    return document.getElementsByTagName("main")[0].innerHTML = html;
  };

  password = null;

  document.getElementById("main-pass-form").addEventListener("submit", function(evt) {
    evt.preventDefault();
    return get_url(db_url, function(data) {
      var err;
      console.log(data);
      console.log("here");
      try {
        console.log(window.database);
        window.database = decipher(document.getElementById("main-pass-input").value, data);
        password = document.getElementById("main-pass-input").value;
        return buildTableView();
      } catch (_error) {
        err = _error;
        console.log(err);
        return document.getElementById("info-p").innerHTML = "Invalid main password!";
      }
    }, function() {
      console.log("Erreur pendant le chargement de la base de données.\n");
      return document.getElementById("info-p").innerHTML = "Unable to load the database";
    });
  });

}).call(this);
